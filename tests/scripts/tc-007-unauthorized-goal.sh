#!/usr/bin/env bash
# TC-007
# Spec: .product/tests/TC-007-*.md
set -euo pipefail

cat >&2 <<MSG
TC-007 not yet implemented.

An unauthorized goal verb must be refused with structured error naming authorized list and ValueAction URI — no Session/Goal/Dispatch written.

The dec binary, oxi-events crate, and code-writer worker are not yet
built. This script is a placeholder so `product verify` finds a failing
runner and the implementation pipeline can pick the TC up.
MSG
exit 1
