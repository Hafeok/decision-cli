//! `dec` — decision-cli binary entry point.
//!
//! Slice 1 surface: init, status, health, implement, events, session.
//! See ADR-011 (CLI shape) and FT-012.

fn main() {
    eprintln!("decision-cli: slice 1 not yet implemented");
    std::process::exit(1);
}
